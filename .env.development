# Esper Development Environment Configuration
# This file contains default values for local development

# Environment
ESPER_ENVIRONMENT=development
LOG_LEVEL=DEBUG

# Core service URLs (localhost for development)
URZA_URL=http://localhost:8000
TAMIYO_URL=http://localhost:8001
TEZZERET_URL=http://localhost:8002

# Redis/Oona configuration
REDIS_URL=redis://localhost:6379/0

# S3/Storage configuration (MinIO for development)
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=esper-artifacts
S3_ACCESS_KEY=minio_admin
S3_SECRET_KEY=minio_password

# Timeout and retry configuration (relaxed for development)
HTTP_TIMEOUT=30
RETRY_ATTEMPTS=3

# Cache and performance settings (smaller for development)
CACHE_SIZE_MB=128
MAX_CACHE_ENTRIES=64

# Polling configuration (more frequent for development testing)
POLL_INTERVAL_SECONDS=5

# Database connection pooling (smaller for development)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=20
DB_POOL_RECYCLE=1800